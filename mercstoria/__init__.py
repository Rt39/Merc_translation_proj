"""Merc Storia translation toolkit — shared library.

Submodules:
    config       — game/cache paths, AES key derivation, PE helpers, constants
    memorypack   — full-schema MemoryPack Reader/Writer for story + master data
"""
