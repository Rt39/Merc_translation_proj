# Python
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info
.venv/

# Bundled third-party tools (download separately)
Il2CppDumper/
il2cpp_output/
tools/

# Procmon / debugging trace artifacts (large, ephemeral)
procmon_logs/

# Live working-state for cron-driven iterations (regenerated each run)
STATE.md

# Binary data (large, regeneratable)
*.bin
*.bundle
*.bundle.bak
*.bundle.original
test_repacked*.bundle
!logofont.bundle

# Font assets (large, distribution-restricted)
*.ttf

# Extracted data (large, regeneratable by `mercstoria extract`)
merc_storia_dialogue.json
merc_metadata.json
jp_monobehaviours.txt
extracted_data/
repacked_bundles/
masterdata_scan.json

# Generic scratch-script patterns. Anything ad-hoc / exploratory matching
# these prefixes is left out of git unless explicitly `git add -f`'d.
analyze_*.py
disasm_*.py
trace_*.py
*_scratch.py
*_example.py

# Launcher build artifacts (CMake)
launcher/build/
